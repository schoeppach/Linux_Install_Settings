Verzeichnis erstellen und Git initialisieren:
 
 mkdir -p Uebung/Test1
 cd Uebung/Test1
 git init
 git config --global user.name "Master"
 git config --global user.email "ridculy@gmx.de"
 ll
 touch index.html
 nano index.html
 
  <html>
  <body>
  <h1>Ein erster Test</h1>
  </body>
  </html>
  
 firefox index.html

 git add index.html
 git commit -m "Initialer Commit"
 ll .git/
 cat .git/HEAD
 cat .git/refs/heads/master
 ll .git/objects/8d
 ll
 git checkout -- index.html (änderung löschen)
 cat index.html (änderung checken)
 git remote add origin https://gitlab/Master/test1.git
 
 vorher Zertifikat im Browser einfügen
 sudo mv ~/Dokumente/gitlab.crt /etc/ssl/certs
 chmod a+r /etc/ssl/certs/gitlab.crt
 ll /etc/ssl/certs/gitlab.crtgit
 
 git push -u origin master
 
 im Browser Datei ändern:
 git pull
 danach zeigt es änderungen an
 
 git log
 
 Neuen Branch erstellen:
 
 git checkout -b feature_x (neuer Branch)

 git checkout master (Branch wechseln)
 
 git add *
 git commit -m "Test geil gemacht"
 
 git diff master feature_x (veränderung sehen)

(zusammen mergen)

git branch
git checkout feature_x
git branch feature_x

git branch -D feature_x (löschen)
git branch
git pull
git fetch

GitKraken:


