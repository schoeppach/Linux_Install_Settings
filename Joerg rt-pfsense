Trerminal:
git config --global init.defaultBranch
cd Uebung/
mkdir rt-pfsense
cd rt-pfsense

Gadget flow installieren: 
sudo apt install git-flow

git config --global init.defaultBranch main
git init
git status

code . 

VSC:
README.md erstellen
1. # rt-pfsense 
2. leer 
3. __ein Boarder-Gateway, das auf pfSense basiert__ 
4. leer
5. eingerichtet mittels Vagrant als IaC-Lösung

Terminal:
git flow init
git branch
git flow feature start vagrant ( neuer Branch) git flow feature finish

VSC:
# -*- mode: ruby -*-

# -*- mode: ruby -*-

Vagrant.configure("2") do |config|
    config.vm.define "rt-pfsense" do |rtpfsense|
        rtpfsense.vm.box = "kennyl/pfsense"
        rtpfsense.vm.hostname = "rt-pfsense"
        rtpfsense.vm.box_version = "2.4.0"
        rtpfsense.vm.synced_folder ".", "/vagrant", disabled: true
        rtpfsense.vm.network "private_network", ip: "192.168.100.1"
        rtpfsense.vm.provider "virtualbox" do |vb|
            vb.name = "rt-b"
            vb.cpus = "2"
            vb.memory = "2048"
        end
    end
end
speichern


